!!!
%html
  %head
    %title ComradeTweet
    = stylesheet_link_tag    "application", :media => "all"
    = javascript_include_tag "application"
    = csrf_meta_tags
  %body
    .navbar.navbar-fixed-top
      .navbar-inner
        .container
          = link_to("Comrade Tweet", root_path, :class => "brand")
          %div
            - if current_user
              %ul.nav
                - active = current_page?(edit_user_path(current_user)) ? 'active' : ''
                %li{ :class => active }
                  = link_to 'Settings', edit_user_path(current_user)
          .pull-right
            %span.user-menu
              - if current_user
                Hi, #{current_user.name}
                = link_to "Sign out", signout_path
              - else
                = link_to "Sign in with Twitter", "/auth/twitter?use_authorize=true"
    .container.main.well
      = yield