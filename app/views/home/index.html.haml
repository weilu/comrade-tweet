%h2 Comrade Tweet!

%header
  %span.user_menu
    - if current_user
      Hi, #{current_user.name}
      = link_to "Sign out", signout_path
    - else
      = link_to "Sign in with Twitter", '/auth/twitter'